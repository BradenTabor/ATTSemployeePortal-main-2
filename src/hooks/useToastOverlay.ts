/**
 * useToastOverlay Hook
 * 
 * Custom hook to access the toast overlay context.
 * Exported from a separate file to avoid fast refresh warnings.
 */

export { useToastOverlay } from '../components/ui/ToastOverlay/useToastOverlay';
export type { ToastOverlayContextValue } from '../components/ui/ToastOverlay/types';
